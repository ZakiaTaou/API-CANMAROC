# Server
PORT=5000
NODE_ENV=development

# Database
# For local PostgreSQL use: DB_HOST=localhost
# For Docker Compose use: DB_HOST=db
DB_HOST=db
DB_PORT=5432
DB_NAME=can_2025
DB_USER=postgres
DB_PASSWORD=your_password

# JWT
JWT_SECRET=your_jwt_secret
JWT_EXPIRE=7d

# CORS
CORS_ORIGIN=http://localhost:3000